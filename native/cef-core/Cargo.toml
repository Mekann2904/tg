[package]
name = "kitty-cef-core"
version = "0.1.0"
edition = "2021"
description = "Core protocol, frame pipeline, and input state for kitty-web-ui CEF backend"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
libc = "0.2"

[lib]
crate-type = ["staticlib"]
